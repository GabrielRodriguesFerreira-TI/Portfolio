import { StaticImageData } from "next/image";

export interface iSectionOptions {
  image: string;
  index: number;
  projectName: string;
  description: string;
  link: string;
  sectionIndex: number;
  projectTechs: Array<any>;
}
